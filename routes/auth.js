// Authentication routes removed — app runs anonymously.
// No login, register, or JWT logic required.
const express = require("express");
const router = express.Router();

module.exports = router;
